# Wiki Lint Report (2026-04-06)

## Summary

| Severity | Count |
|----------|-------|
| 🔴 Error | 1 |
| 🟡 Warning | 2 |
| 🔵 Info | 3 |

## 🔴 Error

### 3層 vs 4相の用語混在 (wiki-knowledge-architecture.md)

`wiki-knowledge-architecture.md` が **3層アーキテクチャ**（Ingest → Compile → Index のデータフロー）と **4相パイプライン**（Ingest → Compile → Query → Lint のワークフロー操作）を混在して記述しており、読者を混乱させる。`querylog.md` は「4相パイプライン」として参照している。

**推奨修正**: wiki-knowledge-architecture.md に「3層モデルはデータフロー、4相パイプラインはワークフロー操作」の明確な区別セクションを追加する。

## 🟡 Warning

### 1. 低 Trust Score 記事 (< 0.30)

| 記事 | Score | 原因 |
|------|-------|------|
| `llm-wiki-tooling` | 0.00 | ソース1件、backlink なし、inbound wikilink なし |
| `llm-wiki-use-cases` | 0.00 | ソース1件、backlink なし、inbound wikilink なし |

両記事は lint-wiki.py でも orphan として検出されている。他記事から `[[llm-wiki-tooling]]` / `[[llm-wiki-use-cases]]` で言及するか、ソースを追加して品質を強化すべき。

### 2. Orphan 記事（inbound wikilink なし）

- `llm-wiki-use-cases` — どの記事からも `[[llm-wiki-use-cases]]` で参照されていない
- `llm-wiki-tooling` — どの記事からも `[[llm-wiki-tooling]]` で参照されていない

`related` フロントマターでは参照されているが、本文中の `[[wikilink]]` がない。

## 🔵 Info

### 1. Dead link（false positive）

以下の `[[wikilink]]` 検出は **コード例・フォーマット説明中のリテラル文字列** であり、実際のリンクではない:

- `querylog.md`: 正規表現パターンの説明で `[[wikilink]]` を文字通り使用
- `trust-score.md`: slug 正規化の例示で `[[wikilink]]` と `[[foo]]` を使用
- `wiki-knowledge-architecture.md`: 記法説明で `[[wikilink]]` を使用

**対応不要**（lint-wiki.py のコードリテラル検出は既知の limitation）

### 2. Gap Detection

QueryLog データなし — ギャップ検出はデータ蓄積後に有効になる。

### 3. Coverage Gap（Auto Ingest 提案）

`gap-detection.md` 内で Auto Ingest 提案が詳しく説明されているが、独立記事はない。現時点では gap-detection に含まれる範囲で十分だが、機能が拡張された場合は分離を検討。

---

## チェック結果一覧

| チェック | Status |
|---------|--------|
| Dead links (lint-wiki.py) | ⚠️ 4件検出（全て false positive） |
| Orphan articles | 🟡 2件 |
| Missing sources | ✅ PASS |
| Trust Score (< 0.30) | 🟡 2件 |
| Gap Detection | ✅ ギャップなし（QueryLog 空） |
| 矛盾検出 (LLM) | 🔴 1件 |
| 陳腐化 (LLM) | ✅ PASS（全記事 2日以内） |
| カバレッジギャップ (LLM) | 🔵 1件（Auto Ingest） |
| フォーマット違反 (LLM) | ✅ PASS |
